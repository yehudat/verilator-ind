╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║     VERILATOR + GTKWAVE DOCKER SETUP                         ║
║     Ready-to-use Verilog development environment             ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

👋 WELCOME!

You now have a complete Docker setup with:
  • Verilator 5.030 (latest stable) - Verilog/SystemVerilog simulator
  • GTKWave 3.3.104 - Waveform viewer
  • Working example project
  • Comprehensive documentation

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 QUICKEST START (3 commands):

   1. docker build -t verilator-gtkwave:latest .
   2. chmod +x test.sh && ./test.sh
   3. docker-compose run --rm verilator

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 DOCUMENTATION GUIDE:

   First time?         → Read OVERVIEW.md
   Want details?       → Read README.md
   Need to verify?     → Read CHECKLIST.md
   Having issues?      → Read TESTING.md
   Just browsing?      → Read INDEX.txt

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚡ TRY THE EXAMPLE (inside container):

   cd /project
   make sim      # Run simulation
   make waves    # View waveforms (requires X11)
   make clean    # Clean build files

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ VERIFICATION:

   The test.sh script will verify:
     ✓ Verilator is installed and working
     ✓ GTKWave is available
     ✓ Example simulation runs
     ✓ VCD files are generated

   Expected result: All tests pass with green checkmarks

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📁 KEY FILES:

   Dockerfile           → Image definition
   docker-compose.yml   → Container config
   test.sh             → Verification script
   project/            → Example Verilog project
     ├─ counter.v      → Simple counter module
     ├─ counter_tb.v   → Testbench
     └─ Makefile       → Build commands

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  IMPORTANT NOTES:

   1. Docker must be running (test with: docker info)
   2. First build takes 5-10 minutes (compiles Verilator)
   3. For GTKWave GUI, you need X11 forwarding setup
   4. The example is ready to run - no modifications needed

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🐛 TROUBLESHOOTING:

   Build fails?        → Check TESTING.md section "Build fails"
   No GUI?            → Check TESTING.md section "GTKWave won't open"
   Permission error?   → Run: chmod -R 755 project/
   Need help?         → See TESTING.md for complete guide

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 NEXT STEPS:

   1. Run test.sh to verify everything works
   2. Try the example project
   3. Modify counter.v to experiment
   4. Create your own Verilog modules
   5. Check README.md for advanced features

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 REMEMBER:

   This setup was created with best practices but I couldn't test it
   with actual Docker execution. The test.sh script will verify
   everything works correctly on your system.

   If something doesn't work, TESTING.md has detailed troubleshooting!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Ready to start? Run: ./test.sh

